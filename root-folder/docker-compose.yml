version: "3.9"

networks:
  homelab-web:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

services:
  # Placeholder test service to validate network
  net-test:
    image: alpine:3.20
    command: ["sh", "-c", "sleep infinity"]
    networks:
      - homelab-web
    restart: unless-stopped